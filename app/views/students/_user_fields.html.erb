<div class="fields">
	<div class="min_user">
 			<%= f.input :email%>
			<%= f.input :password%>
			<%= f.input :password_confirmation%>
			<% if can? :create, @student%>
				<%= f.input :role, :collection => User::ROLES, :prompt => true %>
				<%= f.input :active %>
			<% end %>
	</div>
</div>